# Steam 游戏大数据分析平台 — 启动指南

## 前置条件

- VM（master-pc）：Kafka、Spark、ZooKeeper、Hadoop
- Windows：MySQL 8.0、Tomcat 9.0、JDK 8
- JDK 8 永久配置已写入 `~/.bashrc`

## 一、Windows 上

### 1. 启动 MySQL
```cmd
net start MySQL80
```

### 2. 启动 Tomcat（IDEA）
- **运行 → 编辑配置 → Tomcat**
- Deployment 选 `web\target\steam-web-dashboard.war`
- Application Context: `/steam-web-dashboard`
- 启动

---

## 二、VM 上（按顺序）

### 1. 启动 Hadoop（如果没起）
```bash
cd ~
./cmd-hadoop-launch.sh
```

### 2. 启动 ZooKeeper
```bash
cd ~/kafka_2.12-2.5.0
bin/zookeeper-server-start.sh config/zookeeper.properties &
sleep 3
jps | grep Quorum    # 确认看到 QuorumPeerMain
```

### 3. 启动 Kafka（3 个 Broker）
```bash
bin/kafka-server-start.sh config/server-1.properties &
bin/kafka-server-start.sh config/server-2.properties &
bin/kafka-server-start.sh config/server-3.properties &
sleep 5
jps | grep Kafka      # 确认看到 3 个 Kafka 进程
```
### 备注：如果 Kafka 报InconsistentClusterId，清理 meta.properties：

rm -f /tmp/kafka-logs-*/meta.properties
### 4. 启动数据模拟器（Producer）
```bash
cd ~
./kafka_producer.sh master-pc:9091,master-pc:9092,master-pc:9093 5 &
```

### 5. 清理检查点 + 运行 Spark

**只跑实时流（跳过离线分析，立即可用）：**
```bash
rm -rf /data/member1/checkpoints/realtime_stats
spark-submit --class com.spark.project.Main --master local[*] --driver-memory 1g ~/Sem6SparkProject-1.0.0.jar --streaming-only
```

**完整运行（离线分析 + 实时流）：**
```bash
rm -rf /data/member1/checkpoints/realtime_stats
spark-submit --class com.spark.project.Main --master local[*] --driver-memory 1g ~/Sem6SparkProject-1.0.0.jar --sample --streaming
```
> `--streaming-only` 直接启动实时流，秒级出数据
> `--sample --streaming` 先跑 4 个离线分析（A3 FP-Growth 较慢需等 1-2 分钟），再启动实时流

---

## 三、浏览器访问

```
http://localhost:8080/steam-web-dashboard
```

---

## 快速验证命令

```bash
# 所有进程
jps

# 期望看到：
# QuorumPeerMain (ZK)
# Kafka × 3
# NameNode, DataNode, ResourceManager, NodeManager (Hadoop)
```

```bash
# Kafka Topic 检查
cd ~/kafka_2.12-2.5.0
bin/kafka-topics.sh --list --bootstrap-server master-pc:9091
```

---

## 重新打包（代码改动后）

**Windows cmd：**
```cmd
set JAVA_HOME=D:\jdk-8
cd /d C:\Users\hp\Desktop\sem-6project\sem-6project && mvn clean package -DskipTests
```
新的 JAR 在 `target\Sem6SparkProject-1.0.0.jar`，拖到 VM `~` 覆盖。

**Web 模块：**
```cmd
cd /d C:\Users\hp\Desktop\sem-6project\sem-6project\web && mvn clean package -DskipTests
```
WAR 在 `web\target\steam-web-dashboard.war`。

---

## 停止服务

```bash
pkill -f kafka
pkill -f zookeeper
cd ~ && ./cmd-hadoop-stop.sh
```
